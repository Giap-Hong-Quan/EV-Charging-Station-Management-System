// import express from "express";
// import chargingSessionRouter from "./routes/chargingSession.js";
// const app = express();
// app.use(express.json());

// app.use("/api/v1/charging_sessions", chargingSessionRouter);

// export default app;
